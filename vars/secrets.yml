---
# Admin email
admin_email: "{{ vault_email }}"

### AWS
aws_region: "{{ vault_aws_region }}"

### Mysql
# MySQL user
db_user: "{{ vault_db_user }}"
db_user_password: "{{ vault_db_user_password }}"

# MySQL root user
mysql_root_user: "{{ vault_mysql_root_user }}"
mysql_root_password: "{{ vault_mysql_root_password }}"

### WP installation
wp_user: "{{ vault_wp_user }}"
wp_user_password: "{{ vault_wp_user_password }}"
